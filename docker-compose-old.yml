version: "3.3"

services:

  ws:
    image: postgres:13.0-alpine
    environment:
      POSTGRES_DB: deribit
      POSTGRES_USER: deribit_user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"

  adminer:
    image: adminer
    ports:
      - "8080:8080"